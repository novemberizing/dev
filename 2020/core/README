__NOVEMBERIZING'S CORE LIB__
============================

## 코드스타일

메인 객체의 파라미터는 `o`
객체를 동적할당하는 함수의 이름에는 `x[module]new`
객체를 해제하는 함수의 이름에는 `x[module]rem`
객체를 초기화하는 함수의 이름에는 `x[module]init`
객체의 자원을 반납하고 초기화하는 함수의 이름은 `x[module]term`