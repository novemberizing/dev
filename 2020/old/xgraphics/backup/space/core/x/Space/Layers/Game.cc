#include "Game.hh"

namespace xSpace
{
    namespace Layers
    {
        Game::Game(const x::string & name)
        :   x::graphics::layer(name)
        {
        }

        Game::~Game()
        {
        }
    }
}
