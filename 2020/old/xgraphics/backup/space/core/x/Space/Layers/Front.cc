#include "Front.hh"

namespace xSpace
{
    namespace Layers
    {
        Front::Front(const x::string & name)
        :   x::graphics::layer(name)
        {
        }

        Front::~Front()
        {
        }
    }
}
