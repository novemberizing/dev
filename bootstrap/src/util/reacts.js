
export default class Reacts {
    static display(condition, element) {
        return condition && element;
    }
    
    static children(chlidren, callback) {
        React.Children(childrem, (o) => {
            
        });
    }
}